1. [github pages](https://ltgxs.github.io/Start)
2. 本次调研与优化（最小改动）：
   - 代码优化：增加 CDN `preconnect/dns-prefetch`，并将外部脚本改为 `defer`，降低阻塞加载风险。
   - 新功能/美化增强：
     - 主题切换（深色/浅色）并持久化到 `localStorage`。
     - 天气图标动态化（晴/雨/雪/阴）与对应动画效果。
     - 时间区域增加动态渐变光晕，随小时变化色相。
     - 新增快捷键帮助面板（`?`），并支持 `/` 聚焦搜索、`S` 打开设置、`B` 打开书签、`Esc` 关闭面板。
3. 后续可继续增强（提案）：
   - 增加无障碍增强（例如高对比模式、键盘焦点可视化）。
   - 增加搜索历史/常用词统计卡片（可关闭）。
   - 增加天气与背景联动主题（晴天/雨天自动氛围色）。
